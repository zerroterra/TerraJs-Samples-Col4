import { Coins } from '../Coins';
import { Int } from '../numeric';
import { JSONSerializable } from '../../util/json';
import { CommunityPoolSpendProposal } from '../distribution/proposals';
import { ParameterChangeProposal } from '../params/proposals';
import { TextProposal } from './proposals';
import { SoftwareUpgradeProposal, CancelSoftwareUpgradeProposal } from '../upgrade/proposals';
/**
 * Stores information pertaining to a submitted proposal, such as its status and time of
 * the voting period
 */
export declare class Proposal extends JSONSerializable<Proposal.Data> {
    id: number;
    content: Proposal.Content;
    status: Proposal.Status;
    final_tally_result: Proposal.FinalTallyResult | undefined;
    submit_time: Date;
    deposit_end_time: Date;
    total_deposit: Coins;
    voting_start_time: Date;
    voting_end_time: Date;
    /**
     *
     * @param id proposal's ID
     * @param content content of the proposal
     * @param status proposal's status
     * @param final_tally_result tally result
     * @param submit_time time proposal was submitted and deposit period started
     * @param deposit_end_time time deposit period will end
     * @param total_deposit amount of coins deposited by all users
     * @param voting_start_time time voting period will start
     * @param voting_end_time time voting period will end
     */
    constructor(id: number, content: Proposal.Content, status: Proposal.Status, final_tally_result: Proposal.FinalTallyResult | undefined, submit_time: Date, deposit_end_time: Date, total_deposit: Coins, voting_start_time: Date, voting_end_time: Date);
    static fromData(data: Proposal.Data): Proposal;
    toData(): Proposal.Data;
}
export declare namespace Proposal {
    interface FinalTallyResult {
        yes: Int;
        abstain: Int;
        no: Int;
        no_with_veto: Int;
    }
    type Content = TextProposal | CommunityPoolSpendProposal | ParameterChangeProposal | SoftwareUpgradeProposal | CancelSoftwareUpgradeProposal;
    namespace Content {
        type Data = TextProposal.Data | CommunityPoolSpendProposal.Data | ParameterChangeProposal.Data | SoftwareUpgradeProposal.Data | CancelSoftwareUpgradeProposal.Data;
        function fromData(data: Proposal.Content.Data): Proposal.Content;
    }
    enum Status {
        NIL = 0,
        DEPOSIT_PERIOD = 1,
        VOTING_PERIOD = 2,
        PASSED = 3,
        REJECTED = 4,
        FAILED = 5
    }
    interface Data {
        content: Content.Data;
        id: string;
        status: Status;
        final_tally_result?: {
            yes: string;
            abstain: string;
            no: string;
            no_with_veto: string;
        };
        submit_time: string;
        deposit_end_time: string;
        total_deposit: Coins.Data;
        voting_start_time: string;
        voting_end_time: string;
    }
}
